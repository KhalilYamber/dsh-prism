# dsh-ux-simple

DSH 界面两档模式插件：简化 / 原生一键切换，工具卡片白话化，降低 DeepSeek Harness 的上手门槛。新手看白话，老手要完整，同一套界面两种读法。

## 为什么做这个

DeepSeek Harness 发布后，社区对它的批评集中在一点：**门槛**。

- 界面新闻：「一切皆插件」的设计十分依赖配置（YAML + 插件 + 效果组件 + 服务），「对高级用户而言功能强大，但对于只想快速用上可用智能代理的人来说，上手门槛较高」
- 极客公园：DSH「对非编程用户不是很友好」，像框架、不像成品，是给开发者的尝鲜版
- 社区开发者：「这玩意鬼才用，我为什么没事要插拔」

DSH 的毛坯房是刻意为之，但「想省事的人」和「要全功能的人」不该被迫接受同一套界面。dsh-ux-simple 用两档模式回应这个矛盾：

| 档位 | 适用的人 | 界面表现 |
|---|---|---|
| **简化** | 想先用起来、不想研究术语的人 | 工具调用呈时间线行（图标 + 白话动作 + 状态），点行展开「交付文档」详情 |
| **原生** | 老手、需要完整信息的人 | 插件零接管，产品原貌原样渲染，一个像素都不改 |

默认原生档，切换一次即生效，刷新页面回原生档。不想用的时候，卸掉插件，界面回到出厂状态，没有任何残留。

## 功能

- **左下角悬浮入口**：显示当前模式，点开菜单切换「简化 / 原生」，菜单内含「隐藏复杂工具」开关
- **简化档 = 时间线行 × 交付文档**：
  - 时间线行：类别图标（📄 文件 / 🖥️ 命令 / 🌐 网络 / 📘 技能 / 📋 清单 / ❓ 提问 / 🎯 目标 / 🤖 子代理 / ⏱️ 后台任务 / 🧩 插件系统，未命中 ⚙️）+ 白话动作（进行中自动加「正在」）+ 行尾状态图标（✓ 完成 / ● 进行中 / ✕ 出错），整行可点击，带 `title` 与 `aria-label`
  - 交付文档详情：点行展开，结果先脱敏再以轻量 Markdown 渲染——`#` 标题、`\| a \| b \|` 表格、``` 代码块、`-` 列表、`**粗体**`、行内代码、commit 哈希高亮；解析失败退化为纯文本，绝不抛异常
  - 隐藏复杂工具：17 个高级工具（目标 / 计划、子代理编排、后台任务、插件系统）默认折叠成一行，点「展开」即展开并打开详情；菜单开关可随时关闭折叠
- **数据脱敏（简化档）**：
  - 路径只显示文件名（`file_path` 等参数用 basename 呈现）
  - `token / secret / password / api_key / authorization` 等敏感参数名永不展示
  - 结果文本中的常见密钥形态（`sk-xxx`、`Bearer xxx`、`key=xxx`）替换为占位符
  - 详情面板只展示白话摘要与脱敏结果，不暴露原始参数
- **原生档 = 产品原貌**：原生档下插件完全不注册任何工具卡片，交还产品原样渲染（含通用卡片）；简化档才注册白话卡片，模式切换时动态注册 / 注销、即时生效
- **白话文案全覆盖**：33 个工具规则表全覆盖白话文案（如 `pwsh` → 「正在电脑上执行一条命令」），实际接管 19 个没有原生卡片的工具；有原生卡片的 `read` / `write` / `web_search` 等保持产品原版

## 设计原则

- **纯展示层**：只改界面呈现，模型输入输出零改动，不影响 agent 的任何工作
- **原生零接管**：原生档不注册任何卡片；简化档也只接管没有原生卡片的工具，不覆盖官方 UI
- **内存态切换**：刷新回原生档，简单、干净、无配置污染
- **跟随主题**：全部使用官方 `--dsw-alias-*` 设计变量，明暗主题自适应

## 安装

需要先装好 [DeepSeek Harness](https://github.com/deepseek-ai/deepseek-harness)（Node.js 22.19+ 或 24+）。

> 未发布到 npm，请克隆本仓库后用本地路径安装：

```sh
npx -y @deepseek-ai/dsh plugin --profile web add <本仓库目录>
```

然后正常启动 Web UI 即可，左下角会出现悬浮入口。

## 使用

1. 启动后点击左下角悬浮按键（显示当前模式）
2. 选择「简化」：工具调用变成时间线行，点行看交付文档详情
3. 菜单里可开关「隐藏复杂工具」（仅简化档生效）
4. 选择「原生」：恢复完整原生界面
5. 刷新页面回到原生档

## 常见问题

**为什么刷新后回到原生档？**
档位存在内存里，这是刻意设计：简化档是临时辅助，不想用的时候刷新即消失，不留任何状态。

**为什么有些工具卡片看起来没变？**
`read`、`write`、`web_search` 等工具官方已有产品级原生卡片，插件不覆盖它们，只接管没有原生卡片的 19 个工具，保证原生体验不被破坏。

**简化档会影响 agent 干活吗？**
不会。插件只改界面显示，模型收到的输入输出与原生完全一致。

**点开详情后表格 / 代码块是什么效果？**
结果文本先脱敏，再按 Markdown 子集渲染：`| a | b |` 表格、``` 代码块、`#` 标题、`-` 列表、`**粗体**`、行内代码与 commit 哈希高亮；任何解析失败都退化为纯文本。

## 架构（全部实现位于 `lib/client.js`）

```
TOOL_RULES        规则表：33 个工具 → 白话文案与参数摘要声明
ARG_NAME_RULES    参数名规则：无显式声明的工具按参数名自动生成摘要
SENSITIVE_KEY     敏感参数名（任何情况下不展示其值）
NATIVE_KEYS       产品原生卡片已占用的 key（插件不接管）
REGISTERED_TOOLS  注册名单 = 规则表 − 原生卡片，自动推导
```

注册是动态的：`tool.call.toolview` 仅在简化档注册（模式切换时注册 / 注销），原生档零注册，产品界面原样呈现。

### 规则表字段

| 字段 | 含义 |
| --- | --- |
| `tools` | 工具名（数组）；多个工具共用一条规则 |
| `doing` / `done` | 进行中 / 完成的白话文案；缺省时自动生成通用文案 |
| `complex` | 标记复杂工具：简化档默认折叠成一行，点击可展开 |
| `noArgs` | 不显示参数摘要（保持原有行为） |
| `arg.pick` | 候选参数键，按序取第一个非空字符串 |
| `arg.mode` | 呈现方式：`file`=路径只留文件名 / `raw`=原文 / `short`=截断（配 `max`）/ `count`=数组计数（配 `unit`）/ `wrap`=（值）包裹 / `fixed`=固定文案 |
| `arg.prefix` | 摘要前缀 |
| `arg.fallback` | 参数缺省时的文案；不填则不显示摘要 |

### 新增一个工具

1. 在 `TOOL_RULES` 加一行，**只写工具名即可**：doing/done 自动生成通用白话文案，参数摘要由 `ARG_NAME_RULES` 按参数名自动生成（如 `file_path` → 「文件：xxx」、`url` → 「链接：xxx」），卡片自动注册。
2. 想更精准时再补 `doing` / `done` / `arg`；想让它默认折叠则加 `complex: true`。
3. 若工具名出现在 `NATIVE_KEYS`（有产品原生卡片），插件保持原版、不接管，无需处理。

## 装配

- `cordis.patch.yml`：bundle patch 注入（`insert ux-simple`）。
- `package.json`：`dsh.client.inject: ["@deepseek-ai/dsh-client-runtime"]`，浏览器半体经 `exports["./client"]` 加载。
- 经共享层 `~/.dsh/profiles/node_modules/dsh-ux-simple` 装配进 web profile；修改 `lib/client.js` 后需同步该副本并刷新页面生效。

## License

MIT
